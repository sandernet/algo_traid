# 📋 ПЕЧАТНЫЙ ЧЕК-ЛИСТ ИСПРАВЛЕНИЙ

## 🔴 БЛОК 1: КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ (Этап 1)

### ☑️ Проблема 1.1: Инициализация символа

```
Файл: src/data_fetcher/data_fetcher.py
Строки: 46
Статус: [ ] НЕ НАЧАТО
```

**Задача:**
- [ ] Строка 46: Удалить `self.symbol = self._detect_symbol_format()`
- [ ] Строка 48: Добавить `self.symbol = None  # Будет установлен в _set_exchange()`
- [ ] Строка 58: Добавить валидацию параметров (exchange.get("EXCHANGE_ID"))
- [ ] Строка 67: Добавить валидацию параметров (coin.get("SYMBOL"))

**Проверить:**
- [ ] `test_init_sets_attributes_correctly` теперь ✅ PASS

**Время:** ⏱️ 15 мин

---

### ☑️ Проблема 1.2: Инициализация в _set_exchange()

```
Файл: src/data_fetcher/data_fetcher.py
Строки: 75-84
Статус: [ ] НЕ НАЧАТО
```

**Задача:**
- [ ] Строка 75: Обновить метод `_set_exchange(self):`
- [ ] Строка 84: Добавить `self.symbol = self._detect_symbol_format()`
- [ ] Строка 85: Обновить логирование с информацией о символе

**Код:**
```python
def _set_exchange(self) -> None:
    try:
        exchange_class = getattr(ccxt, self.exchange_id)
        self.exchange = exchange_class({
            "enableRateLimit": True,
            "options": {"defaultType": self.market_type}
        })
        self.symbol = self._detect_symbol_format()  # ✅ ДОБАВИТЬ
        logger.info(f"Подключение к бирже {self.exchange_id} успешно. Символ: {self.symbol}")
    except AttributeError:
        # ... error handling ...
```

**Проверить:**
- [ ] `_set_exchange()` установит правильный symbol

**Время:** ⏱️ 10 мин

---

### ☑️ Проблема 1.3: Часовые пояса (UTC)

```
Файл: src/data_fetcher/data_fetcher.py
Строки: 135-150
Статус: [ ] НЕ НАЧАТО
```

**Задача:**
- [ ] Строка 138: Заменить `datetime.strptime()` на `pd.Timestamp(..., tz='UTC')`
- [ ] Строка 146: Заменить `int(dt.timestamp() * 1000)` на `dt.value // 1_000_000`
- [ ] Добавить import: `from datetime import timezone` (если нужен)

**Код:**
```python
def _convert_date_to_ms(self, date_str: str, is_end_date: bool = False) -> int:
    try:
        # ✅ НОВЫЙ КОД:
        dt = pd.Timestamp(date_str, tz='UTC')
        if is_end_date:
            dt = dt.replace(hour=23, minute=59, second=59, microsecond=999999)
        ms = dt.value // 1_000_000
        return ms
    except (ValueError, TypeError) as e:
        logger.error(f"Ошибка конвертации даты '{date_str}': {e}")
        raise ValueError(f"Неверный формат даты: {date_str}") from e
```

**Проверить:**
- [ ] `test_convert_date_to_ms_start` теперь ✅ PASS
- [ ] `test_convert_date_to_ms_end` теперь ✅ PASS

**Время:** ⏱️ 15 мин

---

### ☑️ Проблема 1.4: Пути на Windows

```
Файл: src/data_fetcher/data_fetcher.py
Строки: 107-133
Статус: [ ] НЕ НАЧАТО
```

**Задача:**
- [ ] Строка 117: Заменить `path = self.directory+"csv_files"` на `os.path.join(self.directory, "csv_files")`
- [ ] Строка 119: Заменить `path = self.directory+"excel"` на `os.path.join(self.directory, "excel")`
- [ ] Строка 63: В `__init__` нормализовать путь:
  ```python
  self.directory = os.path.normpath(directory)
  if not self.directory.endswith(os.sep):
      self.directory += os.sep
  ```

**Код:**
```python
def _get_export_path(self, timeframe: str, file_extension: str = "csv") -> str:
    if file_extension not in ("csv", "xlsx"):
        raise ValueError(f"Неподдерживаемое расширение: {file_extension}")
    
    subdir_map = {"csv": "csv_files", "xlsx": "excel"}
    subdir = subdir_map[file_extension]
    
    # ✅ НОВЫЙ КОД:
    dir_path = os.path.join(self.directory, subdir)
    
    try:
        os.makedirs(dir_path, exist_ok=True)
    except OSError as e:
        logger.error(f"Ошибка при создании директории: {e}")
        raise
    
    safe_symbol = self.symbol.replace('/', '_').replace(':', '_')
    file_name = f"{safe_symbol}_{timeframe}_{self.exchange_id}_OHLCV.{file_extension}"
    
    return os.path.join(dir_path, file_name)
```

**Проверить:**
- [ ] `test_get_export_path_csv` теперь ✅ PASS
- [ ] `test_get_export_path_xlsx` теперь ✅ PASS

**Время:** ⏱️ 10 мин

---

### ☑️ Проблема 1.5: Патчи в тестах

```
Файл: src/data_fetcher/test/test_data_fetcher.py
Строки: 64, 79
Статус: [ ] НЕ НАЧЯТО
```

**Задача:**
- [ ] Строка 64: Заменить `@patch("src.data_fetcher.ccxt")` на `@patch("src.data_fetcher.data_fetcher.ccxt")`
- [ ] Строка 79: Заменить `@patch("src.data_fetcher.ccxt")` на `@patch("src.data_fetcher.data_fetcher.ccxt")`

**Код:**
```python
@patch("src.data_fetcher.data_fetcher.ccxt")  # ✅ ИСПРАВЛЕННЫЙ ПУТЬ
def test_generic_fetcher_successful(mock_ccxt, data_fetcher):
    # ... тест ...

@patch("src.data_fetcher.data_fetcher.ccxt")  # ✅ ИСПРАВЛЕННЫЙ ПУТЬ
def test_generic_fetcher_empty_response(mock_ccxt, data_fetcher):
    # ... тест ...
```

**Проверить:**
- [ ] `test_generic_fetcher_successful` теперь ✅ PASS
- [ ] `test_generic_fetcher_empty_response` теперь ✅ PASS

**Время:** ⏱️ 5 мин

---

## 📊 ПРОГРЕСС ЭТАПА 1

```
Проблема 1.1 (инициализация символа):    [ ] [ ] [ ]
Проблема 1.2 (_set_exchange):             [ ] [ ] [ ]
Проблема 1.3 (часовые пояса):             [ ] [ ] [ ]
Проблема 1.4 (пути Windows):              [ ] [ ] [ ]
Проблема 1.5 (патчи тестов):              [ ] [ ]

ИТОГО: 0% ────────────────────────────── 100% завершено
```

**Контрольная точка после Этапа 1:**
```bash
cd E:\Developmen\Trading\algo_traid
.\venv\Scripts\python.exe -m pytest src/data_fetcher/test/ -v
```

**Ожидаемый результат:**
```
======================== 12 passed in X.XXs ======================== ✅
```

---

## 🟠 БЛОК 2: ВАЖНЫЕ ИСПРАВЛЕНИЯ (Этап 2)

### После успешного завершения Этапа 1 приступить к:

- [ ] Проблема 2.1: Поддержка нескольких бирж
- [ ] Проблема 2.2: Валидация данных  
- [ ] Проблема 2.3: Улучшенная обработка ошибок
- [ ] Проблема 2.4: Валидация входных параметров

**Время:** ⏱️ 4 часа

---

## 🟡 БЛОК 3: ОБЩИЕ УЛУЧШЕНИЯ (Этап 3)

### После успешного завершения Этапа 2 приступить к:

- [ ] Проблема 3.1: Type hints и docstrings
- [ ] Проблема 3.2: Рефакторинг и constants
- [ ] Проблема 3.3: Интеграционные тесты

**Время:** ⏱️ 4 часа

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

После всех исправлений запустить:

```bash
# 1. Все юнит тесты
pytest src/data_fetcher/test/ -v
✅ Ожидание: 12/12 passed

# 2. Проверка качества кода
flake8 src/data_fetcher/data_fetcher.py
✅ Ожидание: 0 ошибок

# 3. Проверка типов (если добавлены type hints)
mypy src/data_fetcher/data_fetcher.py --strict
✅ Ожидание: 0 ошибок

# 4. Покрытие тестами
pytest src/data_fetcher/test/ --cov=src.data_fetcher --cov-report=term-missing
✅ Ожидание: >= 80% coverage
```

---

## 📝 ЗАМЕТКИ РАЗРАБОТЧИКА

### Где найти нужные строки в коде:

```
__init__ (инициализация):              строки 33-52
_set_exchange (инициализация биржи):   строки 64-84
_detect_symbol_format (формат):        строки 86-104
_get_export_path (пути к файлам):      строки 107-133
_convert_date_to_ms (даты):            строки 135-150
_generic_fetcher (загрузка данных):    строки 152-240+
export_to_csv:                          строки 290-310
export_to_excel:                        строки 330-360
load_from_csv:                          строки 370-415
```

### Как быстро найти строки в VS Code:

1. Нажать `Ctrl+G` (Go to Line)
2. Ввести номер строки
3. Enter

### Как быстро заменить текст в VS Code:

1. Нажать `Ctrl+H` (Find and Replace)
2. Найти: `self.directory+"csv_files"`
3. Заменить: `os.path.join(self.directory, "csv_files")`
4. Replace All

---

## 🎯 БЫСТРАЯ ДИАГНОСТИКА ОШИБОК

Если тест падает после исправлений:

| Падающий тест | Вероятная причина | Решение |
|---|---|---|
| test_init_* | Symbol все еще инициализируется в `__init__` | Проверить, удален ли вызов `_detect_symbol_format()` из `__init__` |
| test_convert_date_* | Не используется UTC | Проверить использование `pd.Timestamp(..., tz='UTC')` |
| test_get_export_path_* | Symbol не исправлен | Убедиться, что инициализация symbol исправлена |
| test_generic_fetcher_* | Патч не работает | Проверить полный путь в `@patch()` |

---

## 🔔 ВАЖНЫЕ НАПОМИНАНИЯ

1. **Сохраняйте файлы** после каждого изменения (Ctrl+S)
2. **Запускайте тесты** после каждого блока исправлений
3. **Используйте git** для отслеживания изменений:
   ```bash
   git add src/data_fetcher/data_fetcher.py
   git commit -m "Fix symbol initialization"
   ```
4. **Читайте error messages** - они помогут понять что не так
5. **Не спешите** - лучше сделать аккуратно чем быстро

---

## 📚 ДОКУМЕНТЫ НА СПРАВКУ

- 📄 [CODE_REVIEW_DATA_FETCHER.md](CODE_REVIEW_DATA_FETCHER.md) - Полный разбор
- 📄 [FIXES_DETAILED.md](FIXES_DETAILED.md) - Конкретные исправления с кодом
- 📄 [PROBLEM_MAP.md](PROBLEM_MAP.md) - Диаграммы и графы
- 📄 [SUMMARY_SHORT.md](SUMMARY_SHORT.md) - Быстрый обзор

---

**Дата создания:** 14 января 2026 г.  
**Статус:** 🟡 Готово к печати и использованию  
**Рекомендация:** Распечатать и повесить рядом с монитором! 🖨️
